# backend-using-Django-preferably-with-Django-REST-Framework-
Scope of Work: 1. Develop the backend using Django (preferably with Django REST Framework). 2. Implement a rating system (e.g., for users, items, or content — details will be provided). 3. Build a REST API to communicate with the frontend (frontend is ready/provided). 4. Set up authentication/authorization (JWT or OAuth2)
